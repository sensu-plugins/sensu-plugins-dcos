---
driver:
  name: docker
  use_sudo: false

provisioner:
  name: shell
  data_path: .
  script: test/fixtures/bootstrap.sh
  volume:
    - /home/travis/.rvm/rubies

verifier:
  ruby_bindir: <%= ENV['MY_RUBY_HOME'] || '/opt/sensu/embedded' %>/bin

platforms:
  - name: ubuntu-16.04

suites:
  - name: default
